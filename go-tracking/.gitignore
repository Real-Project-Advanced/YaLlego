# Environment variables — never commit real credentials
.env

# Compiled binaries
tracking-service
tracking-service.exe

# Go build cache
*.test
*.out

# AI assistant docs — local only, not committed
CLAUDE.md
INTEGRATION.md